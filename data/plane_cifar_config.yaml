name: plane
shiftpatch: False
shiftcrop: True

patch_config:
  patch_filepath:
  bg_filepath:
  patch_size:
  bg_size: 
  stride:

crop_config:
  img_filepath: templates/jet.png
  img_size: (54, 54)
  crop_size: (32,32)
  stride: 1

out_dir: datasets/plane_cifar
