name: ship
shiftpatch: False
shiftcrop: True

patch_config:
  patch_filepath:
  bg_filepath:
  patch_size:
  bg_size: 
  stride:

crop_config:
  img_filepath: templates/ship.png
  img_size: (52,52)
  crop_size: (32,32)
  stride: 1

out_dir: datasets/ship_cifar/sub/
