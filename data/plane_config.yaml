name: plane
shiftpatch: False
shiftcrop: True

patch_config:
  patch_filepath:
  bg_filepath:
  patch_size:
  bg_size: 
  stride:

crop_config:
  img_filepath: templates/jet.png
  img_size: (356,356)
  crop_size: (224,224)
  stride: 1

out_dir: datasets/plane